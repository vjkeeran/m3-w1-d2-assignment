doctype html
html
  head
    meta(charset="utf-8")
    title= title
    style.
      body { font-family: Arial, margin: 2rem; max-width: 800px; }
      .error { color: red; background: #ffe6e6; padding: 0.5rem; border-radius: 4px; }
      form { display: flex; flex-direction: column; gap: 0.5rem; }
      label { font-weight: bold; }
      input { padding: 0.4rem; border: 1px solid #ccc; border-radius: 4px; }
      .field { display: flex; flex-direction: column; }
      .btn { padding: 0.6rem 1rem; background: teal; color: white; border: none; border-radius: 4px; cursor: pointer; width: fit-content; }
  body
    header
      h1 Foodblog Registration
    block content
    footer
      hr
      p &copy; 2025 Foodblog
